@extends('layouts.app')
@section('title', 'お問い合わせ | MUSAKO')
@section('content')
<div class="mx-auto max-w-4xl"><a href="{{ route('student.dashboard') }}" class="text-sm font-semibold text-amber-700">← マイページに戻る</a><p class="mt-4 text-sm font-semibold tracking-widest text-amber-700">CONTACT</p><h1 class="mt-1 text-2xl font-bold sm:text-3xl">その他問い合わせ</h1>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif
<form method="post" action="{{ route('student.inquiries.store') }}" class="mt-6 grid gap-4 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm sm:p-7">@csrf<label class="grid gap-2 text-sm font-semibold">カテゴリ<select name="category" required class="min-h-12 rounded-xl border border-stone-300 bg-white px-3"><option value="">選択してください</option>@foreach ($categories as $category)<option value="{{ $category->value }}" @selected(old('category') === $category->value)>{{ $category->label() }}</option>@endforeach</select></label><label class="grid gap-2 text-sm font-semibold">件名<input name="subject" value="{{ old('subject') }}" required maxlength="255" class="min-h-12 min-w-0 rounded-xl border border-stone-300 px-3"></label><label class="grid gap-2 text-sm font-semibold">本文<textarea name="body" rows="6" required maxlength="5000" class="min-w-0 rounded-xl border border-stone-300 p-3">{{ old('body') }}</textarea></label><button class="min-h-12 rounded-xl bg-stone-900 font-bold text-white">問い合わせを送信する</button></form>
<section class="mt-8"><h2 class="text-xl font-bold">問い合わせ履歴</h2><div class="mt-3 grid gap-3">@forelse ($inquiries as $inquiry)<a href="{{ route('student.inquiries.show', $inquiry) }}" class="min-w-0 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-2"><strong class="break-words">{{ $inquiry->subject }}</strong><span class="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold">{{ $inquiry->status->label() }}</span></div><p class="mt-2 text-sm text-stone-500">{{ $inquiry->category->label() }} · {{ $inquiry->requested_at->format('Y年n月j日 H:i') }}</p></a>@empty<p class="rounded-2xl border border-dashed border-stone-300 p-8 text-center text-sm text-stone-500">問い合わせ履歴はありません。</p>@endforelse</div></section></div>
@endsection
