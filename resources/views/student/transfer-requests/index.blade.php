@extends('layouts.app')
@section('title', '振替申請 | MUSAKO')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-amber-700">TRANSFER</p><h1 class="text-2xl font-bold sm:text-3xl">振替申請履歴</h1><p class="mt-2 text-sm text-stone-600">承認済み予約の「振替を申請」から手続きできます。</p></div><a href="{{ route('student.reservations.index') }}" class="inline-flex min-h-11 items-center justify-center rounded-xl bg-stone-900 px-5 text-sm font-semibold text-white">予約一覧へ</a></div>
<div class="mt-7 grid gap-4">@forelse ($transferRequests as $transferRequest)<article class="rounded-2xl border border-stone-200 bg-white p-4 shadow-sm sm:p-5"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div class="min-w-0"><div class="flex flex-wrap items-center gap-2"><strong>{{ $transferRequest->originalReservationRequest->lessonSlot->starts_at->format('n/j H:i') }} → {{ $transferRequest->requestedLessonSlot->starts_at->format('n/j H:i') }}</strong><span class="rounded-full px-3 py-1 text-xs font-semibold {{ ['pending' => 'bg-amber-100 text-amber-800', 'approved' => 'bg-emerald-100 text-emerald-800', 'rejected' => 'bg-red-100 text-red-700'][$transferRequest->status->value] }}">{{ ['pending' => '承認待ち', 'approved' => '承認済み', 'rejected' => '却下'][$transferRequest->status->value] }}</span></div><p class="mt-2 text-sm text-stone-600">振替先: {{ $transferRequest->requestedLessonSlot->teacherProfile->display_name }} · {{ $transferRequest->requestedLessonSlot->venue?->name ?? '会場未定' }}</p>@if ($transferRequest->staff_note)<p class="mt-2 text-sm text-stone-500">先生から: {{ $transferRequest->staff_note }}</p>@endif</div><time class="text-xs text-stone-500">申請 {{ $transferRequest->requested_at->format('Y/m/d H:i') }}</time></div></article>@empty<div class="rounded-2xl border border-dashed border-stone-300 bg-white p-10 text-center text-stone-500">振替申請はまだありません。</div>@endforelse</div>
<div class="mt-6">{{ $transferRequests->links() }}</div>
@endsection
