@extends('layouts.app')
@section('title', '振替先を選択 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold text-amber-700">TRANSFER REQUEST</p><h1 class="text-2xl font-bold sm:text-3xl">振替先を選択</h1><p class="mt-2 text-sm text-stone-600">申請期限は元レッスン前日の19:00です。</p></div>
@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif
<section class="mt-6 rounded-2xl bg-stone-900 p-5 text-white"><span class="text-xs text-stone-300">元の予約</span><h2 class="mt-1 text-lg font-bold">{{ $reservationRequest->lessonSlot->starts_at->format('Y年n月j日 H:i') }}〜{{ $reservationRequest->lessonSlot->ends_at->format('H:i') }}</h2><p class="mt-2 text-sm text-stone-300">申請期限 {{ $reservationRequest->transferRequestDeadline()->format('Y年n月j日 H:i') }}</p></section>
<form method="post" action="{{ route('student.transfer-requests.store', $reservationRequest) }}" class="mt-6 grid gap-5">@csrf<fieldset><legend class="font-bold">予約可能な振替先</legend><div class="mt-3 grid gap-3 sm:grid-cols-2">@forelse ($availableSlots as $slot)<label class="flex cursor-pointer gap-3 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm has-checked:border-amber-500 has-checked:ring-2 has-checked:ring-amber-200"><input type="radio" name="requested_lesson_slot_id" value="{{ $slot->id }}" required @checked(old('requested_lesson_slot_id') == $slot->id) class="mt-1 size-5"><span><strong class="block">{{ $slot->starts_at->format('n月j日 H:i') }}〜{{ $slot->ends_at->format('H:i') }}</strong><span class="mt-1 block text-sm text-stone-600">{{ $slot->teacherProfile->display_name }} · {{ $slot->venue?->name ?? '会場未定' }}</span><span class="mt-1 block text-xs text-stone-500">{{ $slot->course?->name ?? 'コース指定なし' }} · 残り{{ $slot->capacity - $slot->approved_reservations_count }}名</span></span></label>@empty<div class="rounded-2xl border border-dashed border-stone-300 bg-white p-8 text-center text-stone-500 sm:col-span-2">現在選択できる振替先がありません。</div>@endforelse</div></fieldset><label class="grid gap-2 text-sm font-semibold">理由（任意）<textarea name="reason" rows="3" maxlength="1000" class="rounded-xl border border-stone-300 p-3 font-normal">{{ old('reason') }}</textarea></label><label class="grid gap-2 text-sm font-semibold">備考（任意）<textarea name="student_note" rows="3" maxlength="1000" class="rounded-xl border border-stone-300 p-3 font-normal">{{ old('student_note') }}</textarea></label><div class="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><a href="{{ route('student.reservations.index') }}" class="inline-flex min-h-12 items-center justify-center rounded-xl border border-stone-300 px-5 font-semibold">戻る</a><button @disabled($availableSlots->isEmpty()) class="min-h-12 rounded-xl bg-amber-400 px-6 font-bold text-stone-900 disabled:cursor-not-allowed disabled:bg-stone-200">振替を申請する</button></div></form>
@endsection
