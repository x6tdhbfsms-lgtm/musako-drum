@extends('layouts.app')
@section('title', '支払い方法変更 | MUSAKO')
@section('content')
<div class="mx-auto max-w-4xl"><a href="{{ route('student.dashboard') }}" class="text-sm font-semibold text-amber-700">← マイページに戻る</a><p class="mt-4 text-sm font-semibold tracking-widest text-amber-700">PAYMENT</p><h1 class="mt-1 text-2xl font-bold sm:text-3xl">支払い方法変更</h1><div class="mt-4 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><strong>安全のため、カード番号・銀行口座番号は入力しないでください。</strong><p class="mt-1">この画面では希望する支払い方法と管理上の申請情報だけを保存します。</p></div>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif
<form method="post" action="{{ route('student.payment-method-change-requests.store') }}" class="mt-6 grid gap-4 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm sm:grid-cols-2 sm:p-7">@csrf<label class="grid gap-2 text-sm font-semibold">対象契約<select name="lesson_enrollment_id" class="min-h-12 min-w-0 rounded-xl border border-stone-300 bg-white px-3"><option value="">主契約</option>@foreach ($enrollments as $enrollment)<option value="{{ $enrollment->id }}">{{ $enrollment->course->name }}</option>@endforeach</select></label><label class="grid gap-2 text-sm font-semibold">希望する支払い方法<select name="requested_method" required class="min-h-12 rounded-xl border border-stone-300 bg-white px-3"><option value="">選択してください</option>@foreach ($paymentMethods as $method)<option value="{{ $method->value }}" @selected(old('requested_method') === $method->value)>{{ $method->label() }}</option>@endforeach</select></label><label class="grid gap-2 text-sm font-semibold sm:col-span-2">備考（機密情報は入力しないでください）<textarea name="student_note" rows="3" maxlength="1000" class="rounded-xl border border-stone-300 p-3">{{ old('student_note') }}</textarea></label><button class="min-h-12 rounded-xl bg-stone-900 font-bold text-white sm:col-span-2">変更を申請する</button></form>
<section class="mt-8"><h2 class="text-xl font-bold">申請履歴</h2><div class="mt-3 grid gap-3">@forelse ($requests as $item)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex flex-wrap justify-between gap-2"><strong>{{ $item->requested_method->label() }}</strong><span class="rounded-full px-3 py-1 text-xs font-bold {{ ['pending'=>'bg-amber-100 text-amber-900','approved'=>'bg-emerald-100 text-emerald-800','rejected'=>'bg-red-100 text-red-800'][$item->status->value] }}">{{ ['pending'=>'申請中','approved'=>'承認済み','rejected'=>'却下'][$item->status->value] }}</span></div>@if ($item->rejection_reason)<p class="mt-2 text-sm text-red-700">却下理由：{{ $item->rejection_reason }}</p>@endif</article>@empty<p class="rounded-2xl border border-dashed border-stone-300 p-8 text-center text-sm text-stone-500">申請履歴はありません。</p>@endforelse</div></section></div>
@endsection
