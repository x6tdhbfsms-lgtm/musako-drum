@extends('layouts.app')
@section('title', '予約一覧 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold text-amber-700">MY RESERVATIONS</p><h1 class="text-2xl font-bold sm:text-3xl">予約一覧</h1><p class="mt-2 text-sm text-stone-600">申請状況と確定したレッスンを確認できます。</p></div>
<div class="mt-7 grid gap-4">@forelse ($reservations as $reservation)<article class="rounded-2xl border border-stone-200 bg-white p-4 shadow-sm sm:p-5"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div class="flex flex-wrap items-center gap-2"><strong class="text-lg">{{ $reservation->lessonSlot->starts_at->format('Y年n月j日 H:i') }}〜{{ $reservation->lessonSlot->ends_at->format('H:i') }}</strong><span class="rounded-full px-3 py-1 text-xs font-semibold {{ ['pending' => 'bg-amber-100 text-amber-800', 'approved' => 'bg-emerald-100 text-emerald-800', 'rejected' => 'bg-red-100 text-red-700', 'cancelled' => 'bg-stone-200 text-stone-600'][$reservation->status->value] }}">{{ ['pending' => '承認待ち', 'approved' => '予約確定', 'rejected' => '却下', 'cancelled' => 'キャンセル済み'][$reservation->status->value] }}</span></div><p class="mt-2 text-sm text-stone-600">{{ $reservation->lessonSlot->teacherProfile->display_name }} · {{ $reservation->lessonSlot->course?->name ?? 'コース指定なし' }} · {{ $reservation->lessonSlot->venue?->name ?? '会場未定' }}</p>@if ($reservation->staff_note)<p class="mt-2 text-sm text-stone-500">先生から: {{ $reservation->staff_note }}</p>@endif</div>@if (in_array($reservation->status->value, ['pending','approved'], true) && $reservation->lessonSlot->starts_at->isFuture())<form method="post" action="{{ route('student.reservations.destroy', $reservation) }}" class="flex flex-col gap-2 sm:min-w-56">@csrf @method('delete')<input name="cancellation_reason" maxlength="500" placeholder="理由（任意）" class="min-h-11 rounded-xl border border-stone-300 px-3 text-sm"><button onclick="return confirm('この予約をキャンセルしますか？')" class="min-h-11 rounded-xl border border-red-200 px-4 text-sm font-semibold text-red-700 hover:bg-red-50">予約をキャンセル</button></form>@endif</div></article>@empty<div class="rounded-2xl border border-dashed border-stone-300 bg-white p-12 text-center"><p class="text-stone-500">予約はまだありません。</p><a href="{{ route('student.lesson-slots.index') }}" class="mt-4 inline-flex min-h-11 items-center rounded-xl bg-stone-900 px-5 font-semibold text-white">空き枠を探す</a></div>@endforelse</div><div class="mt-6">{{ $reservations->links() }}</div>
@endsection
