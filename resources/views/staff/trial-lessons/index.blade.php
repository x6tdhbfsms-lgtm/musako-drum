@extends('layouts.app')
@section('title', '体験レッスン申込み | MUSAKO')
@section('content')
<div class="space-y-6"><div class="flex flex-wrap items-end justify-between gap-3"><div><p class="text-sm font-bold tracking-widest text-amber-700">TRIAL LESSONS</p><h1 class="mt-1 text-3xl font-bold">体験レッスン申込み</h1></div><a href="{{ route('staff.admission-applications.index') }}" class="rounded-xl border border-stone-300 bg-white px-4 py-3 text-sm font-bold">入会申込みを見る</a></div>
<form class="flex max-w-sm gap-2"><select name="status" class="min-h-11 min-w-0 grow rounded-xl border border-stone-300 bg-white px-3"><option value="">すべて</option>@foreach (App\Enums\TrialLessonStatus::cases() as $case)<option value="{{ $case->value }}" @selected($status === $case->value)>{{ $case->label() }}</option>@endforeach</select><button class="rounded-xl bg-stone-900 px-4 text-sm font-bold text-white">絞り込み</button></form>
<div class="grid gap-3">@forelse ($requests as $item)<a href="{{ route('staff.trial-lessons.show', $item) }}" class="grid gap-3 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm hover:border-amber-400 sm:grid-cols-[1fr_auto] sm:items-center"><div class="min-w-0"><div class="flex flex-wrap items-center gap-2"><strong>{{ $item->name }}</strong><span class="rounded-full bg-amber-100 px-2.5 py-1 text-xs font-bold">{{ $item->status->label() }}</span>@if ($item->admissionApplication)<span class="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-800">入会申込みあり</span>@endif</div><p class="mt-2 text-sm text-stone-600">{{ $item->lessonSlot->starts_at->format('Y/n/j H:i') }}／{{ $item->lessonSlot->teacherProfile->display_name }}／{{ $item->lessonSlot->venue?->name ?? '会場未定' }}</p></div><span class="text-sm font-bold text-amber-800">詳細 →</span></a>@empty<p class="rounded-2xl bg-white p-6 text-sm text-stone-500">該当する体験申込みはありません。</p>@endforelse</div>{{ $requests->links() }}</div>
@endsection
