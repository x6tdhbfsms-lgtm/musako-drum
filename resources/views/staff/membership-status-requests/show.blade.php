@extends('layouts.app')
@section('title', '在籍申請詳細 | MUSAKO')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-amber-700">MEMBERSHIP DETAIL</p><h1 class="text-2xl font-bold sm:text-3xl">{{ $membershipStatusRequest->studentProfile->user->name }}さんの{{ ['pause' => '休会', 'withdraw' => '退会', 'resume' => '再開'][$membershipStatusRequest->type->value] }}申請</h1></div><a href="{{ route('staff.membership-status-requests.index') }}" class="text-sm font-semibold text-stone-600">← 一覧へ</a></div>
@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif
<div class="mt-6 grid gap-4 md:grid-cols-2"><section class="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><h2 class="font-bold">申請内容</h2><dl class="mt-4 grid gap-3 text-sm"><div><dt class="text-stone-500">希望日</dt><dd class="mt-1 font-semibold">{{ $membershipStatusRequest->effective_on->format('Y年n月j日') }}</dd></div><div><dt class="text-stone-500">理由</dt><dd class="mt-1">{{ $membershipStatusRequest->reason ?: '未入力' }}</dd></div><div><dt class="text-stone-500">備考</dt><dd class="mt-1">{{ $membershipStatusRequest->student_note ?: '未入力' }}</dd></div></dl></section><section class="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><h2 class="font-bold">現在の在籍契約</h2><div class="mt-4 grid gap-3">@forelse ($membershipStatusRequest->studentProfile->enrollments as $enrollment)<div class="rounded-xl bg-stone-50 p-3"><strong class="block text-sm">{{ $enrollment->course->name }}</strong><span class="mt-1 block text-xs text-stone-500">{{ ['pending' => '手続き中', 'active' => '在籍中', 'paused' => '休会中', 'ended' => '終了'][$enrollment->status->value] }}</span></div>@empty<p class="text-sm text-stone-500">在籍契約がありません。</p>@endforelse</div></section></div>
@if ($membershipStatusRequest->status->value === 'pending')<form method="post" action="{{ route('staff.membership-status-requests.update', $membershipStatusRequest) }}" class="mt-5 grid gap-3 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm sm:grid-cols-[1fr_auto_auto]">@csrf @method('patch')<input name="staff_note" maxlength="1000" placeholder="生徒へのメモ（任意）" class="min-h-12 rounded-xl border border-stone-300 px-3"><button name="decision" value="rejected" class="min-h-12 rounded-xl border border-red-200 px-5 font-semibold text-red-700">却下</button><button name="decision" value="approved" class="min-h-12 rounded-xl bg-emerald-600 px-6 font-semibold text-white">承認</button></form>@else<div class="mt-5 rounded-2xl bg-stone-100 p-5 text-sm"><strong>{{ $membershipStatusRequest->status->value === 'approved' ? '承認済み' : '却下' }}</strong><p class="mt-1 text-stone-600">{{ $membershipStatusRequest->reviewer?->name }} · {{ $membershipStatusRequest->reviewed_at?->format('Y/m/d H:i') }}</p>@if ($membershipStatusRequest->status->value === 'approved')<p class="mt-2">{{ $membershipStatusRequest->applied_at ? '在籍契約へ反映済みです。' : $membershipStatusRequest->effective_on->format('Y年n月j日').'に反映予定です。' }}</p>@endif @if ($membershipStatusRequest->staff_note)<p class="mt-2">{{ $membershipStatusRequest->staff_note }}</p>@endif</div>@endif
@endsection
