@extends('layouts.app')
@section('title', '在籍申請管理 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold text-amber-700">MEMBERSHIP REQUESTS</p><h1 class="text-2xl font-bold sm:text-3xl">在籍申請管理</h1><p class="mt-2 text-sm text-stone-600">休会・退会・再開の申請を確認します。</p></div>
<div class="mt-7 grid gap-4">@forelse ($membershipStatusRequests as $membershipStatusRequest)<a href="{{ route('staff.membership-status-requests.show', $membershipStatusRequest) }}" class="rounded-2xl border {{ $membershipStatusRequest->status->value === 'pending' ? 'border-amber-300' : 'border-stone-200' }} bg-white p-4 shadow-sm hover:border-amber-400 sm:p-5"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div class="flex flex-wrap items-center gap-2"><strong>{{ $membershipStatusRequest->studentProfile->user->name }}</strong><span class="rounded-full bg-stone-100 px-3 py-1 text-xs font-semibold">{{ ['pause' => '休会', 'withdraw' => '退会', 'resume' => '再開'][$membershipStatusRequest->type->value] }}</span><span class="rounded-full px-3 py-1 text-xs font-semibold {{ ['pending' => 'bg-amber-100 text-amber-800', 'approved' => 'bg-emerald-100 text-emerald-800', 'rejected' => 'bg-red-100 text-red-700'][$membershipStatusRequest->status->value] }}">{{ ['pending' => '承認待ち', 'approved' => '承認済み', 'rejected' => '却下'][$membershipStatusRequest->status->value] }}</span></div><p class="mt-2 text-sm text-stone-600">希望日 {{ $membershipStatusRequest->effective_on->format('Y年n月j日') }}</p></div><span class="text-sm font-semibold text-amber-700">詳細を確認 →</span></div></a>@empty<div class="rounded-2xl border border-dashed border-stone-300 bg-white p-10 text-center text-stone-500">在籍申請はありません。</div>@endforelse</div><div class="mt-6">{{ $membershipStatusRequests->links() }}</div>
@endsection
