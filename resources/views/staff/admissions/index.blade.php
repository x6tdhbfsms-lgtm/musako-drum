@extends('layouts.app')
@section('title', '入会申込み管理 | MUSAKO')
@section('content')
<div class="space-y-6"><div><p class="text-sm font-bold tracking-widest text-emerald-700">ADMISSIONS</p><h1 class="mt-1 text-3xl font-bold">入会申込み管理</h1></div><form class="flex max-w-sm gap-2"><select name="status" class="min-h-11 grow rounded-xl border border-stone-300 bg-white px-3"><option value="">すべて</option>@foreach (App\Enums\AdmissionApplicationStatus::cases() as $case)<option value="{{ $case->value }}" @selected($status === $case->value)>{{ $case->label() }}</option>@endforeach</select><button class="rounded-xl bg-stone-900 px-4 text-sm font-bold text-white">絞り込み</button></form><div class="grid gap-3">@forelse ($applications as $item)<a href="{{ route('staff.admission-applications.show', $item) }}" class="grid gap-3 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm sm:grid-cols-[1fr_auto] sm:items-center"><div><div class="flex flex-wrap gap-2"><strong>{{ $item->name }}</strong><span class="rounded-full bg-emerald-100 px-2 py-1 text-xs font-bold">{{ $item->status->label() }}</span></div><p class="mt-2 text-sm text-stone-600">{{ $item->course->name }}／{{ $item->lesson_type->label() }}／月{{ $item->monthly_lesson_count }}回</p></div><span class="text-sm font-bold">詳細 →</span></a>@empty<p class="rounded-2xl bg-white p-6 text-sm text-stone-500">入会申込みはありません。</p>@endforelse</div>{{ $applications->links() }}</div>
@endsection
