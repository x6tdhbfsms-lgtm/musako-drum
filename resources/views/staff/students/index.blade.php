@extends('layouts.app')
@section('title', '生徒一覧 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold tracking-widest text-amber-700">STUDENTS</p><h1 class="mt-1 text-2xl font-bold sm:text-3xl">生徒一覧</h1><p class="mt-2 text-sm text-stone-600">契約・予約・各種申請を一画面で確認できます。</p></div><div class="mt-6 grid gap-4 sm:grid-cols-2">@forelse ($students as $student)<a href="{{ route('staff.students.show', $student) }}" class="min-w-0 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-2"><strong class="text-lg">{{ $student->user->name }}</strong><span class="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold">{{ $student->user->account_status->value === 'active' ? '在籍' : $student->user->account_status->value }}</span></div><p class="mt-3 text-sm text-stone-600">{{ $student->enrollments->pluck('course.name')->filter()->join('、') ?: '有効な契約なし' }}</p><p class="mt-1 text-xs text-stone-500">{{ $student->phone ?: '電話番号未登録' }}</p></a>@empty<p class="rounded-2xl border border-dashed border-stone-300 bg-white p-8 text-center text-sm text-stone-500 sm:col-span-2">表示できる生徒はいません。</p>@endforelse</div><div class="mt-6">{{ $students->links() }}</div>
@endsection
