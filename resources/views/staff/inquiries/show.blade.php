@extends('layouts.app')
@section('title', 'お問い合わせ詳細 | MUSAKO')
@section('content')
<div class="mx-auto max-w-4xl"><a href="{{ route('staff.inquiries.index') }}" class="text-sm font-semibold text-amber-700">← お問い合わせ一覧</a><article class="mt-5 min-w-0 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm sm:p-7"><div class="flex flex-wrap items-center justify-between gap-2"><span class="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold">{{ $inquiry->status->label() }}</span><span class="text-xs text-stone-500">{{ $inquiry->requested_at->format('Y年n月j日 H:i') }}</span></div><p class="mt-5 text-sm text-stone-500">{{ $inquiry->studentProfile->user->name }} · {{ $inquiry->category->label() }}</p><h1 class="mt-1 break-words text-2xl font-bold">{{ $inquiry->subject }}</h1><p class="mt-5 whitespace-pre-wrap break-words text-sm leading-7">{{ $inquiry->body }}</p><a href="{{ route('staff.students.show', $inquiry->studentProfile) }}" class="mt-5 inline-flex min-h-11 items-center rounded-xl border border-stone-300 px-4 text-sm font-bold">生徒詳細</a></article>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('staff.inquiries.update', $inquiry) }}" class="mt-5 grid gap-4 rounded-2xl border border-stone-200 bg-white p-5 sm:p-7">@csrf @method('patch')<label class="grid gap-2 text-sm font-semibold">対応状態<select name="status" class="min-h-12 rounded-xl border border-stone-300 bg-white px-3">@foreach (App\Enums\InquiryStatus::cases() as $status)<option value="{{ $status->value }}" @selected($inquiry->status === $status)>{{ $status->label() }}</option>@endforeach</select></label><label class="grid gap-2 text-sm font-semibold">生徒に表示するメモ<textarea name="staff_note" rows="4" maxlength="2000" class="rounded-xl border border-stone-300 p-3">{{ old('staff_note', $inquiry->staff_note) }}</textarea></label><button class="min-h-12 rounded-xl bg-stone-900 font-bold text-white">状態を更新する</button></form></div>
@endsection
