@extends('layouts.app')
@section('title', 'お問い合わせ管理 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold tracking-widest text-amber-700">INQUIRIES</p><h1 class="mt-1 text-2xl font-bold sm:text-3xl">お問い合わせ管理</h1><p class="mt-2 text-sm text-stone-600">生徒から届いたお問い合わせの対応状況を管理します。</p></div><div class="mt-6 grid gap-3">@forelse ($inquiries as $inquiry)<a href="{{ route('staff.inquiries.show', $inquiry) }}" class="min-w-0 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-2"><strong class="break-words">{{ $inquiry->subject }}</strong><span class="rounded-full px-3 py-1 text-xs font-bold {{ $inquiry->status->value === 'open' ? 'bg-amber-100 text-amber-900' : ($inquiry->status->value === 'in_progress' ? 'bg-sky-100 text-sky-800' : 'bg-emerald-100 text-emerald-800') }}">{{ $inquiry->status->label() }}</span></div><p class="mt-2 text-sm text-stone-600">{{ $inquiry->studentProfile->user->name }} · {{ $inquiry->category->label() }}</p><p class="mt-1 text-xs text-stone-400">{{ $inquiry->requested_at->format('Y年n月j日 H:i') }}</p></a>@empty<p class="rounded-2xl border border-dashed border-stone-300 bg-white p-8 text-center text-sm text-stone-500">お問い合わせはありません。</p>@endforelse</div><div class="mt-6">{{ $inquiries->links() }}</div>
@endsection
