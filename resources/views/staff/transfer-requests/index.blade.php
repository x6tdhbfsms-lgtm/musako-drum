@extends('layouts.app')
@section('title', '振替申請管理 | MUSAKO')
@section('content')
<div><p class="text-sm font-semibold text-amber-700">TRANSFER REQUESTS</p><h1 class="text-2xl font-bold sm:text-3xl">振替申請管理</h1><p class="mt-2 text-sm text-stone-600">承認待ちを確認し、詳細画面から処理します。</p></div>
<div class="mt-7 grid gap-4">@forelse ($transferRequests as $transferRequest)<a href="{{ route('staff.transfer-requests.show', $transferRequest) }}" class="rounded-2xl border {{ $transferRequest->status->value === 'pending' ? 'border-amber-300' : 'border-stone-200' }} bg-white p-4 shadow-sm hover:border-amber-400 sm:p-5"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div class="flex flex-wrap items-center gap-2"><strong>{{ $transferRequest->studentProfile->user->name }}</strong><span class="rounded-full px-3 py-1 text-xs font-semibold {{ ['pending' => 'bg-amber-100 text-amber-800', 'approved' => 'bg-emerald-100 text-emerald-800', 'rejected' => 'bg-red-100 text-red-700'][$transferRequest->status->value] }}">{{ ['pending' => '承認待ち', 'approved' => '承認済み', 'rejected' => '却下'][$transferRequest->status->value] }}</span></div><p class="mt-2 text-sm font-semibold">{{ $transferRequest->originalReservationRequest->lessonSlot->starts_at->format('n/j H:i') }} → {{ $transferRequest->requestedLessonSlot->starts_at->format('n/j H:i') }}</p></div><span class="text-sm font-semibold text-amber-700">詳細を確認 →</span></div></a>@empty<div class="rounded-2xl border border-dashed border-stone-300 bg-white p-10 text-center text-stone-500">振替申請はありません。</div>@endforelse</div><div class="mt-6">{{ $transferRequests->links() }}</div>
@endsection
