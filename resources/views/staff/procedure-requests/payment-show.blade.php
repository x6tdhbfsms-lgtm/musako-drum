@extends('layouts.app')
@section('title', '支払い方法変更申請 | MUSAKO')
@section('content')
<div class="mx-auto max-w-3xl"><a href="{{ route('staff.procedure-requests.index') }}" class="text-sm font-semibold text-amber-700">← 各種手続き管理</a><p class="mt-4 text-sm font-semibold tracking-widest text-amber-700">PAYMENT</p><h1 class="mt-1 text-2xl font-bold">支払い方法変更申請</h1>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif<section class="mt-6 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><dl class="grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-stone-500">生徒</dt><dd class="mt-1 font-semibold">{{ $paymentMethodChangeRequest->studentProfile->user->name }}</dd></div><div><dt class="text-stone-500">対象契約</dt><dd class="mt-1 font-semibold">{{ $paymentMethodChangeRequest->lessonEnrollment?->course?->name ?? '主契約' }}</dd></div><div><dt class="text-stone-500">変更前</dt><dd class="mt-1 font-semibold">{{ $paymentMethodChangeRequest->previous_method?->label() ?? '未設定' }}</dd></div><div><dt class="text-stone-500">希望方法</dt><dd class="mt-1 font-semibold">{{ $paymentMethodChangeRequest->requested_method->label() }}</dd></div></dl><p class="mt-5 rounded-xl bg-amber-50 p-3 text-sm text-amber-900">カード番号・銀行口座番号はこのシステムに保存していません。</p><a href="{{ route('staff.students.show', $paymentMethodChangeRequest->studentProfile) }}" class="mt-4 inline-flex min-h-11 items-center rounded-xl border border-stone-300 px-4 text-sm font-bold">生徒詳細</a></section>@include('staff.procedure-requests._review-form', ['application' => $paymentMethodChangeRequest, 'action' => route('staff.payment-method-change-requests.update', $paymentMethodChangeRequest)])</div>
@endsection
