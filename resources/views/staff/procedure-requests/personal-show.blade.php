@extends('layouts.app')
@section('title', '個人情報変更申請 | MUSAKO')
@section('content')
@php($labels = ['name'=>'氏名','email'=>'メールアドレス','phone'=>'電話番号','address'=>'住所'])
<div class="mx-auto max-w-4xl"><a href="{{ route('staff.procedure-requests.index') }}" class="text-sm font-semibold text-amber-700">← 各種手続き管理</a><p class="mt-4 text-sm font-semibold tracking-widest text-amber-700">PERSONAL INFORMATION</p><h1 class="mt-1 text-2xl font-bold">{{ $personalInformationChangeRequest->studentProfile->user->name }}さんの変更申請</h1>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif<section class="mt-6 overflow-hidden rounded-2xl border border-stone-200 bg-white shadow-sm"><div class="grid grid-cols-[5.5rem_1fr_1fr] bg-stone-100 p-3 text-xs font-bold sm:grid-cols-[9rem_1fr_1fr]"><span>項目</span><span>変更前</span><span>変更後</span></div>@foreach ($labels as $key => $label)<div class="grid min-w-0 grid-cols-[5.5rem_1fr_1fr] border-t border-stone-100 p-3 text-sm sm:grid-cols-[9rem_1fr_1fr]"><strong>{{ $label }}</strong><span class="min-w-0 break-words text-stone-500">{{ $personalInformationChangeRequest->before_values[$key] ?: '未登録' }}</span><span class="min-w-0 break-words font-semibold">{{ $personalInformationChangeRequest->after_values[$key] ?: '未登録' }}</span></div>@endforeach</section><a href="{{ route('staff.students.show', $personalInformationChangeRequest->studentProfile) }}" class="mt-4 inline-flex min-h-11 items-center rounded-xl border border-stone-300 px-4 text-sm font-bold">生徒詳細</a>@include('staff.procedure-requests._review-form', ['application' => $personalInformationChangeRequest, 'action' => route('staff.personal-information-change-requests.update', $personalInformationChangeRequest)])</div>
@endsection
