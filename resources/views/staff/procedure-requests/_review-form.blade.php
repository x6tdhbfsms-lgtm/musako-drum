@if ($application->status->value === 'pending')<form method="post" action="{{ $action }}" class="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5">@csrf @method('patch')<h2 class="font-bold">申請を処理</h2><label class="mt-4 grid gap-2 text-sm font-semibold">却下理由（却下時は必須）<textarea name="rejection_reason" rows="3" maxlength="1000" class="rounded-xl border border-stone-300 bg-white p-3">{{ old('rejection_reason') }}</textarea></label><div class="mt-4 grid grid-cols-2 gap-3"><button name="decision" value="rejected" class="min-h-12 rounded-xl border border-red-300 bg-white font-bold text-red-700">却下</button><button name="decision" value="approved" class="min-h-12 rounded-xl bg-emerald-600 font-bold text-white">承認</button></div></form>@elseif ($application->rejection_reason)<div class="mt-6 rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><strong>却下理由</strong><p class="mt-2 break-words">{{ $application->rejection_reason }}</p></div>@endif
