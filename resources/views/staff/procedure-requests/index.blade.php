@extends('layouts.app')
@section('title', '各種手続き管理 | MUSAKO')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold tracking-widest text-amber-700">PROCEDURES</p><h1 class="mt-1 text-2xl font-bold sm:text-3xl">各種手続き管理</h1><p class="mt-2 text-sm text-stone-600">契約・個人情報・支払い方法の申請を一元管理します。</p></div><span class="self-start rounded-full bg-amber-100 px-4 py-2 text-sm font-bold text-amber-900">承認待ち {{ $pendingCount }}件</span></div>
@php($statusLabels = ['pending'=>'申請中','approved'=>'承認済み','rejected'=>'却下'])
@foreach ([['契約内容変更', $contractRequests, 'staff.contract-change-requests.show'], ['個人情報変更', $personalRequests, 'staff.personal-information-change-requests.show'], ['支払い方法変更', $paymentRequests, 'staff.payment-method-change-requests.show']] as [$heading, $items, $routeName])<section class="mt-8"><h2 class="text-xl font-bold">{{ $heading }}</h2><div class="mt-3 grid gap-3">@forelse ($items as $item)<a href="{{ route($routeName, $item) }}" class="min-w-0 rounded-2xl border border-stone-200 bg-white p-4 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-2"><strong>{{ $item->studentProfile->user->name }}</strong><span class="rounded-full px-3 py-1 text-xs font-bold {{ $item->status->value === 'pending' ? 'bg-amber-100 text-amber-900' : ($item->status->value === 'approved' ? 'bg-emerald-100 text-emerald-800' : 'bg-red-100 text-red-800') }}">{{ $statusLabels[$item->status->value] }}</span></div><p class="mt-2 text-sm text-stone-600">@if ($item instanceof App\Models\ContractChangeRequest){{ $item->type->label() }} · 希望日 {{ $item->effective_on->format('Y/n/j') }}@elseif ($item instanceof App\Models\PaymentMethodChangeRequest){{ $item->requested_method->label() }}@else氏名・連絡先の変更@endif</p><p class="mt-1 text-xs text-stone-400">{{ $item->requested_at->format('Y年n月j日 H:i') }}</p></a>@empty<p class="rounded-2xl border border-dashed border-stone-300 bg-white p-6 text-center text-sm text-stone-500">申請はありません。</p>@endforelse</div></section>@endforeach
@endsection
