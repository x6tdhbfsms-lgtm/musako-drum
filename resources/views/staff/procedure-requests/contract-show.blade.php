@extends('layouts.app')
@section('title', '契約変更申請詳細 | MUSAKO')
@section('content')
@php($weekdays = ['日曜日','月曜日','火曜日','水曜日','木曜日','金曜日','土曜日'])
<div class="mx-auto max-w-4xl"><a href="{{ route('staff.procedure-requests.index') }}" class="text-sm font-semibold text-amber-700">← 各種手続き管理</a><div class="mt-4 flex flex-wrap items-end justify-between gap-3"><div><p class="text-sm font-semibold tracking-widest text-amber-700">CONTRACT REQUEST</p><h1 class="mt-1 text-2xl font-bold">{{ $contractChangeRequest->type->label() }}</h1><p class="mt-2 text-sm text-stone-600">{{ $contractChangeRequest->studentProfile->user->name }}さん</p></div><span class="rounded-full bg-stone-100 px-3 py-1 text-sm font-bold">{{ ['pending'=>'申請中','approved'=>'承認済み','rejected'=>'却下'][$contractChangeRequest->status->value] }}</span></div>@if ($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif
<section class="mt-6 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm sm:p-7"><h2 class="text-lg font-bold">申請内容</h2><dl class="mt-4 grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-stone-500">希望適用日</dt><dd class="mt-1 font-semibold">{{ $contractChangeRequest->effective_on->format('Y年n月j日') }}</dd></div><div><dt class="text-stone-500">対象契約</dt><dd class="mt-1 font-semibold">{{ $contractChangeRequest->lessonEnrollment?->course?->name ?? '新しいコース' }}</dd></div>@if (isset($contractChangeRequest->after_values['weekday']))<div><dt class="text-stone-500">希望曜日</dt><dd class="mt-1 font-semibold">{{ $weekdays[$contractChangeRequest->after_values['weekday']] }}</dd></div><div><dt class="text-stone-500">希望時間</dt><dd class="mt-1 font-semibold">{{ mb_substr($contractChangeRequest->after_values['starts_at_time'], 0, 5) }}〜</dd></div>@endif @if (isset($contractChangeRequest->after_values['monthly_lesson_limit']))<div><dt class="text-stone-500">希望月回数</dt><dd class="mt-1 font-semibold">{{ $contractChangeRequest->after_values['monthly_lesson_limit'] }}回</dd></div>@endif @if (isset($contractChangeRequest->after_values['lesson_minutes']))<div><dt class="text-stone-500">希望時間</dt><dd class="mt-1 font-semibold">{{ $contractChangeRequest->after_values['lesson_minutes'] }}分</dd></div>@endif @if ($requestedCourse)<div><dt class="text-stone-500">希望コース</dt><dd class="mt-1 font-semibold">{{ $requestedCourse->name }}</dd></div>@endif @if ($requestedVenue)<div><dt class="text-stone-500">希望会場</dt><dd class="mt-1 font-semibold">{{ $requestedVenue->name }}</dd></div>@endif</dl>@if ($contractChangeRequest->student_note)<div class="mt-5 rounded-xl bg-stone-100 p-4 text-sm"><strong>備考</strong><p class="mt-2 whitespace-pre-wrap break-words">{{ $contractChangeRequest->student_note }}</p></div>@endif<a href="{{ route('staff.students.show', $contractChangeRequest->studentProfile) }}" class="mt-5 inline-flex min-h-11 items-center rounded-xl border border-stone-300 px-4 text-sm font-bold">生徒詳細</a></section>
@include('staff.procedure-requests._review-form', ['application' => $contractChangeRequest, 'action' => route('staff.contract-change-requests.update', $contractChangeRequest)])</div>
@endsection
