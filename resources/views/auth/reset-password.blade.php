@extends('layouts.app')
@section('title', '初回パスワード設定 | MUSAKO')
@section('content')
<form method="post" action="{{ route('password.update') }}" class="mx-auto max-w-md space-y-5 rounded-3xl border border-stone-200 bg-white p-6 shadow-sm sm:p-8">@csrf<input type="hidden" name="token" value="{{ $token }}"><h1 class="text-2xl font-bold">初回パスワード設定</h1>@if ($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-800">{{ $errors->first() }}</div>@endif<label class="grid gap-2 text-sm font-semibold">メールアドレス<input type="email" name="email" required value="{{ old('email', $email) }}" class="min-h-12 rounded-xl border border-stone-300 px-3"></label><label class="grid gap-2 text-sm font-semibold">新しいパスワード<input type="password" name="password" required autocomplete="new-password" class="min-h-12 rounded-xl border border-stone-300 px-3"></label><label class="grid gap-2 text-sm font-semibold">新しいパスワード（確認）<input type="password" name="password_confirmation" required autocomplete="new-password" class="min-h-12 rounded-xl border border-stone-300 px-3"></label><button class="min-h-12 w-full rounded-xl bg-stone-900 font-bold text-white">パスワードを設定する</button></form>
@endsection
